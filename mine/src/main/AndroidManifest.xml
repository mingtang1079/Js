<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mine">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">

        <activity android:name=".me.RealNameVerifyActivity"></activity>
        <activity android:name=".me.UserInfoActivity"></activity>
        <activity android:name=".me.ChangeSexActivity" />
        <activity android:name=".me.ChangeNameActivity" />
        <activity android:name=".me.ChangeUserHeadActivity" />

    </application>

</manifest>
